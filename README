- build exe
  + on windows
  + gnu make is required

- build stub
  + linux required
  + misc\podman\rebuild-stubs\Dockerfile
    ```
      dpkg --add-architecture i386 && apt-get update && apt-get upgrade -y
      apt-get install - --no-install-recommends libc6:i386 zlib1g:i386 g++-multilib gcc-multilib
      curl 'https://github.com/upx/upx-stubtools/releases/download/v20221212/bin-upx-20221212.tar.xz' -O
      tar -xoaf bin-upx-20221212.tar.xz /C ~/.local/bin/
      rm bin-upx-20221212.tar.xz
    ```
